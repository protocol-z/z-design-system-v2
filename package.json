{
  "name": "z-design-system-v2",
  "private": true,
  "version": "0.1.0",
  "description": "Design system, tokens, and component library for Z Protocol surfaces (v2 / Serene System).",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/protocol-z/z-design-system-v2.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "preview": "python3 -m http.server 4173",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "sync:enterprise": "node scripts/sync-enterprise-foundations.mjs --write",
    "check:enterprise-sync": "node scripts/sync-enterprise-foundations.mjs --check"
  },
  "devDependencies": {
    "@types/react": "^19.2.2",
    "react": "^19.2.0",
    "typescript": "^5.8.3"
  }
}
